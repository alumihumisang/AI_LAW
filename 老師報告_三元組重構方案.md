# 知識圖譜重構方案：從「存原文」到「三元組推理」

## 一、現狀分析：你目前的圖譜結構

### 目前設計（存原文為主）

```
結構 1：律師輸入鏈
Case -[HAS_LAWYER_INPUT]-> LawyerInput_Accident (description: "原告於...")
                              ↓ [導致]
                         LawyerInput_Injury (description: "原告受有...")
                              ↓ [請求]
                         LawyerInput_Claim (description: "請求被告賠償...")

結構 2：細節層次樹
Case -[包含]-> Facts -[HAS_DETAIL]-> FactDetail (chunk_text: "...", extracted_info: {...})
                 ↓ [適用]
              Laws -[HAS_DETAIL]-> LawDetail (chunk_text: "...", article: "民法184-1")
                 ↓ [計算]
              Compensation -[HAS_DETAIL]-> CompensationDetail (chunk_text: "...")
```

### 問題所在

1. **原文太多**：每個節點存大段文字（100-500字），佔空間
2. **推理困難**：
   - 想找「闖紅燈導致骨折的案例」→ 需要搜尋所有 chunk_text
   - 想找「民法184適用於哪些情境」→ 需要讀所有 LawDetail
   - 想做因果推理（行為→傷害→賠償）→ 根本做不到

3. **不像知識圖譜**：更像是「文件資料庫」+「層次結構」，沒有利用到圖的推理能力

---

## 二、三元組設計的優勢

### TAARN 的三元組範例

```
(Ilya Sutskever) -[advisor]-> (Geoffrey Hinton)
(Geoffrey Hinton) -[works_at]-> (University of Toronto)

推理：Ilya → advisor → Hinton → works_at → Toronto
查詢：「Ilya 的導師在哪工作？」→ 2-hop 路徑推理
```

### 如果你的案例也是三元組

**目前的問題**：
```
Case_3062 的 FactDetail.chunk_text = "被告張三於 2023/1/1 駕駛機車闖紅燈，
與原告李四的汽車相撞，導致李四右腿骨折..."
```
→ 要找「闖紅燈→骨折」需要文字搜尋，不能推理

**三元組改造後**：
```
(Case_3062) -[has_perpetrator]-> (張三)
(張三) -[violated]-> (闖紅燈)
(張三) -[驾驶]-> (機車)
(張三) -[caused_accident]-> (Accident_3062)
(Accident_3062) -[caused_injury]-> (骨折)
(骨折) -[injured_party]-> (李四)
(骨折) -[body_part]-> (右腿)
(Case_3062) -[applies_law]-> (民法184-1)
(Case_3062) -[awarded_compensation]-> (醫療費:50000)
```

**強大的推理能力**：
- Query 1：「找出所有闖紅燈導致骨折的案例」
  ```cypher
  MATCH (c:Case)-[:has_perpetrator]->(p)-[:violated]->(v:Violation {type: '闖紅燈'})
  MATCH (c)-[:caused_injury]->(i:Injury {type: '骨折'})
  RETURN c
  ```

- Query 2：「闖紅燈通常適用什麼法條？」
  ```cypher
  MATCH (p)-[:violated]->(:Violation {type: '闖紅燈'})
  MATCH (c)-[:has_perpetrator]->(p)
  MATCH (c)-[:applies_law]->(law)
  RETURN law.article, count(*) as frequency
  ORDER BY frequency DESC
  ```

- Query 3：「骨折的平均賠償金額」
  ```cypher
  MATCH (i:Injury {type: '骨折'})<-[:caused_injury]-(c)
  MATCH (c)-[:awarded_compensation]->(comp)
  RETURN avg(comp.amount)
  ```

**這才是知識圖譜的威力！**

---

## 三、重構方案：三選一

### 方案 1：完全重構（最酷，但工作量最大）

**做法**：用 LLM 提取所有三元組，刪除原文節點

#### 新的 Schema 設計

**實體類型**（節點標籤）：
```
- Case: 案例主節點
- Person: 當事人（原告/被告）
- Vehicle: 車輛（機車/汽車/卡車）
- Violation: 違規行為（闖紅燈/超速/酒駕）
- Injury: 傷害（骨折/腦震盪/死亡）
- BodyPart: 受傷部位（右腿/頭部/左手）
- Law: 法條（民法184-1）
- Compensation: 賠償項目（醫療費/精神慰撫金）
- Location: 地點（路口名稱）
- DateTime: 時間（事故發生時間）
```

**關係類型**：
```
因果關係：
- violated: (Person)-[violated]->(Violation)  張三闖紅燈
- caused_accident: (Person)-[caused_accident]->(Accident)  張三造成事故
- caused_injury: (Accident)-[caused_injury]->(Injury)  事故導致骨折
- injured_party: (Injury)-[injured_party]->(Person)  骨折的受害者是李四
- at_body_part: (Injury)-[at_body_part]->(BodyPart)  骨折在右腿

屬性關係：
- driving: (Person)-[driving]->(Vehicle)  張三駕駛機車
- at_location: (Accident)-[at_location]->(Location)  事故發生在XX路口
- at_time: (Accident)-[at_time]->(DateTime)  事故發生時間

法律關係：
- applies_law: (Case)-[applies_law]->(Law)  案例適用民法184
- has_plaintiff: (Case)-[has_plaintiff]->(Person)  案例的原告
- has_defendant: (Case)-[has_defendant]->(Person)  案例的被告
- awarded: (Case)-[awarded]->(Compensation)  判決賠償醫療費
```

#### 具體例子：案例 3062 的三元組表示

**原文**：
```
被告張三於 2023/1/1 上午 10 點，駕駛機車行經台北市忠孝東路與復興南路口時，
因闖紅燈與原告李四駕駛的汽車相撞，導致李四右腿骨折，
判決被告應賠償醫療費 50,000 元、精神慰撫金 100,000 元。
```

**三元組拆解**（共 20+ 個三元組）：
```
# 當事人
(Case_3062) -[has_defendant]-> (張三)
(Case_3062) -[has_plaintiff]-> (李四)

# 事故行為
(張三) -[driving]-> (機車)
(李四) -[driving]-> (汽車)
(張三) -[violated]-> (闖紅燈)
(張三) -[caused_accident]-> (Accident_3062)

# 地點時間
(Accident_3062) -[at_location]-> (忠孝東路復興南路口)
(Accident_3062) -[at_time]-> (2023-01-01T10:00)

# 傷害
(Accident_3062) -[caused_injury]-> (骨折_3062)
(骨折_3062) -[injured_party]-> (李四)
(骨折_3062) -[at_body_part]-> (右腿)

# 法律適用
(Case_3062) -[applies_law]-> (民法184-1)
(Case_3062) -[applies_law]-> (民法191-2)

# 賠償
(Case_3062) -[awarded]-> (醫療費_3062 {amount: 50000})
(Case_3062) -[awarded]-> (精神慰撫金_3062 {amount: 100000})
```

#### 工作量評估

| 任務 | 工作量 | 說明 |
|------|--------|------|
| 設計 Schema | 1 天 | 定義所有實體和關係類型 |
| 寫提取腳本 | 2 天 | 用 LLM 提取三元組 |
| 處理 6057 筆案例 | 3-5 天 | API 調用 + 錯誤修正 |
| 刪除舊節點 | 0.5 天 | 清理舊的 FactDetail 等 |
| 測試查詢 | 1 天 | 驗證推理能力 |
| **總計** | **7-10 天** | 全職工作 |

#### 優缺點

**優點**：
- ✅ 最酷！真正的知識圖譜推理
- ✅ 查詢超快（不用文字搜尋）
- ✅ 可以做統計分析（「闖紅燈通常判多少錢」）
- ✅ 可以做因果推理（「違規行為 → 傷害 → 賠償金額」）
- ✅ 適合給老師展示「酷酷的推理」

**缺點**：
- ❌ 工作量很大（7-10 天）
- ❌ 丟失了原文（無法展示原始文本）
- ❌ 需要高品質的 LLM 提取（會有錯誤）
- ❌ 如果提取錯誤，修正很麻煩

---

### 方案 2：混合式（推薦！）

**核心想法**：原文和三元組並存，各取所長

#### 設計架構

```
兩層結構：

【文本層】（保留原文，用於展示和檢索）
Case -[HAS_LAWYER_INPUT]-> LawyerInput_Accident (description: "...")
Case -[包含]-> Facts -[HAS_DETAIL]-> FactDetail (chunk_text: "...")

【三元組層】（用於推理和統計）
Case -[has_perpetrator]-> (張三:Person)
     -[violated]-> (闖紅燈:Violation)
     -[caused_injury]-> (骨折:Injury)
     -[applies_law]-> (民法184-1:Law)
     -[awarded]-> (醫療費:Compensation {amount: 50000})
```

#### 實際例子：同一個案例的雙層表示

```
案例 3062：

【文本層】（用於展示）
(Case_3062) -[:HAS_LAWYER_INPUT]-> (LawyerInput_Accident)
  LawyerInput_Accident.description = "被告張三於 2023/1/1..."

【三元組層】（用於推理）
(Case_3062) -[:has_defendant]-> (張三:Person)
(張三) -[:violated]-> (闖紅燈:Violation)
(張三) -[:caused_injury]-> (骨折_3062:Injury)
(Case_3062) -[:applies_law]-> (民法184-1:Law)
(Case_3062) -[:awarded]-> (醫療費_3062:Compensation {amount: 50000})
```

#### 使用情境

**情境 1：使用者查詢「闖紅燈導致骨折的案例」**
```cypher
# 用三元組層快速找到案例
MATCH (c:Case)-[:has_defendant]->(p)-[:violated]->(:Violation {type: '闖紅燈'})
MATCH (c)-[:caused_injury]->(:Injury {type: '骨折'})

# 找到後，展示文本層的原文
MATCH (c)-[:HAS_LAWYER_INPUT]->(li)
RETURN c.case_id, li.description
```

**情境 2：統計「骨折的平均賠償」**
```cypher
# 用三元組層做統計
MATCH (c:Case)-[:caused_injury]->(:Injury {type: '骨折'})
MATCH (c)-[:awarded]->(comp:Compensation {item: '醫療費'})
RETURN avg(comp.amount)
```

#### 工作量評估

| 任務 | 工作量 | 說明 |
|------|--------|------|
| 設計三元組 Schema | 1 天 | 定義關鍵實體和關係 |
| 寫提取腳本 | 1-2 天 | 只提取關鍵資訊 |
| 處理 6057 筆案例 | 2-3 天 | 建立三元組層 |
| 保留舊節點 | 0 天 | 不用刪除！ |
| 測試查詢 | 1 天 | 驗證雙層查詢 |
| **總計** | **5-7 天** | 比方案 1 少 |

#### 優缺點

**優點**：
- ✅ 兼顧推理和展示（兩全其美）
- ✅ 工作量較小（只提取關鍵資訊）
- ✅ 不用刪除舊資料（風險低）
- ✅ 錯誤容忍度高（三元組錯了不影響原文）
- ✅ 老師問細節時可以展示原文

**缺點**：
- ⚠️ 圖會比較複雜（兩層結構）
- ⚠️ 儲存空間較大（原文 + 三元組）

---

### 方案 3：局部重構（折衷）

**做法**：只把最關鍵的部分改成三元組，其他保留

#### 選擇性三元組化

**提取成三元組**（影響相似度計算的關鍵資訊）：
1. ✅ 案例類型：`(Case)-[:has_liability_type]->(LiabilityType)`
2. ✅ 法條：`(Case)-[:cites_law]->(Law)`
3. ✅ 賠償項目：`(Case)-[:awards]->(CompensationItem {amount: ...})`
4. ✅ 違規行為：`(Case)-[:involves_violation]->(Violation)`
5. ✅ 傷害類型：`(Case)-[:caused_injury]->(InjuryType)`

**保留原文**（不影響相似度計算的部分）：
- ❌ 事故詳細經過（保留在 FactDetail.chunk_text）
- ❌ 當事人詳細陳述（保留在 LawyerInput.description）
- ❌ 法院判決理由（保留在 Laws.chunk_text）

#### 實際例子

```
案例 3062：

【三元組層】（只提取關鍵資訊）
(Case_3062) -[:has_liability_type]-> (general_tort_184)
(Case_3062) -[:cites_law]-> (民法184-1)
(Case_3062) -[:cites_law]-> (民法191-2)
(Case_3062) -[:involves_violation]-> (闖紅燈)
(Case_3062) -[:caused_injury]-> (骨折)
(Case_3062) -[:awards]-> (醫療費 {amount: 50000})
(Case_3062) -[:awards]-> (精神慰撫金 {amount: 100000})

【原文層】（保留詳細資訊）
(Case_3062) -[:HAS_LAWYER_INPUT]-> (LawyerInput_Accident)
  description = "被告張三於 2023/1/1 上午 10 點，駕駛機車行經..."
```

#### 工作量評估

| 任務 | 工作量 | 說明 |
|------|--------|------|
| 設計簡化 Schema | 0.5 天 | 只定義 5-8 種關係 |
| 寫提取腳本 | 1 天 | 從 extracted_info 提取 |
| 處理 6057 筆案例 | 1-2 天 | 只提取關鍵欄位 |
| 保留舊節點 | 0 天 | 不用刪除 |
| 測試查詢 | 0.5 天 | 驗證相似度查詢 |
| **總計** | **3-4 天** | 最快！ |

#### 優缺點

**優點**：
- ✅ 工作量最小（3-4 天）
- ✅ 滿足 Step 5 的相似度計算需求
- ✅ 保留原文用於展示
- ✅ 不需要複雜的 LLM 提取（大部分已經在 extracted_info 裡了）

**缺點**：
- ⚠️ 推理能力有限（只能推理關鍵資訊）
- ⚠️ 不能做複雜的因果推理（例如「違規行為 → 傷害類型」的因果鏈）

---

## 四、方案比較與推薦

| 維度 | 方案 1（完全重構） | 方案 2（混合式） | 方案 3（局部重構） |
|------|-------------------|-----------------|-------------------|
| **工作量** | 7-10 天 | 5-7 天 | 3-4 天 ⭐ |
| **推理能力** | 最強 ⭐ | 強 | 中等 |
| **展示能力** | 弱（沒原文） | 最強 ⭐ | 強 |
| **風險** | 高（刪除舊資料） | 低 ⭐ | 低 ⭐ |
| **適合 Step 5** | ✅ | ✅ ⭐ | ✅ |
| **適合給老師展示** | ✅（很酷但風險高） | ✅ ⭐（酷且安全） | ⚠️（有點酷） |

### 我的推薦順序

#### 🥇 推薦：方案 2（混合式）

**理由**：
1. 既能做「酷酷的推理」（給老師看），又能保留原文（實用）
2. 工作量適中（5-7 天）
3. 風險低（不刪除舊資料）
4. 未來擴展性強（想加更多三元組就加）

**適合你的情況**：
- 你已經有 `extracted_info`，可以直接轉成三元組
- 你的 Step 5 相似度計算需要結構化資訊
- 老師想看「酷酷的推理」，但你也需要實用的檢索系統

#### 🥈 次選：方案 3（局部重構）

**理由**：
1. 工作量最小（3-4 天），快速完成
2. 足夠支援 Step 5 的相似度計算
3. 可以先做這個，未來再升級到方案 2

**適合你的情況**：
- 時間緊迫，要儘快跟老師討論
- 只需要滿足相似度計算的基本需求
- 可以作為「快速原型」，證明三元組的可行性

#### 🥉 不推薦：方案 1（完全重構）

**理由**：
1. 工作量太大（7-10 天）
2. 風險高（刪除原文後無法展示）
3. 錯誤率高（LLM 提取會有錯誤）

**除非**：
- 老師明確要求「完全用三元組」
- 你有充足的時間和人力
- 你的最終目標是發論文（需要純粹的知識圖譜）

---

## 五、具體實作建議（以方案 2 為例）

### Step 1：設計三元組 Schema（1 天）

#### 定義核心實體

```python
ENTITY_TYPES = {
    'Person': '當事人（原告/被告）',
    'Violation': '違規行為（闖紅燈/超速/酒駕）',
    'Injury': '傷害類型（骨折/腦震盪）',
    'Law': '法條（民法184-1）',
    'Compensation': '賠償項目（醫療費/精神慰撫金）',
    'LiabilityType': '責任類型（guardian_liability_187/general_tort_184_191）',
    'PartyStructure': '當事人結構（single_vs_single/multiple_both）'
}
```

#### 定義核心關係

```python
RELATION_TYPES = {
    # 案例關係
    'has_plaintiff': '原告',
    'has_defendant': '被告',
    'has_liability_type': '責任類型',
    'has_party_structure': '當事人結構',

    # 行為關係
    'violated': '違反規定',
    'caused_injury': '導致傷害',

    # 法律關係
    'cites_law': '引用法條',
    'awards': '判決賠償'
}
```

### Step 2：從現有資料提取三元組（2-3 天）

你已經有 `extracted_info` 了！直接轉換：

```python
# KG_370_build_triples.py

def extract_triples_from_case(case_id):
    """
    從現有的 extracted_info 建立三元組
    """
    with driver.session() as session:
        # 取得案例的結構化資訊
        result = session.run("""
            MATCH (c:Case {case_id: $case_id})

            // 取得法條
            OPTIONAL MATCH (ld:LawDetail {case_id: $case_id})

            // 取得賠償項目
            OPTIONAL MATCH (cd:CompensationDetail {case_id: $case_id})

            RETURN
                c.liability_type,
                c.party_structure,
                collect(DISTINCT ld.article) as laws,
                collect(DISTINCT cd.extracted_info) as compensations
        """, case_id=case_id)

        record = result.single()

        # 建立三元組
        triples = []

        # 1. 責任類型
        if record['liability_type']:
            triples.append({
                'head': f'Case_{case_id}',
                'relation': 'has_liability_type',
                'tail': record['liability_type']
            })

        # 2. 法條
        for law in record['laws']:
            if law:
                triples.append({
                    'head': f'Case_{case_id}',
                    'relation': 'cites_law',
                    'tail': law
                })

        # 3. 賠償項目（從 extracted_info 解析）
        for comp_info in record['compensations']:
            comp_data = json.loads(comp_info)
            if 'item' in comp_data and 'amount' in comp_data:
                triples.append({
                    'head': f'Case_{case_id}',
                    'relation': 'awards',
                    'tail': comp_data['item'],
                    'properties': {'amount': comp_data['amount']}
                })

        return triples

def create_triple_in_neo4j(triple):
    """
    在 Neo4j 建立三元組
    """
    query = """
    MERGE (head {id: $head_id})
    MERGE (tail {id: $tail_id})
    MERGE (head)-[r:$relation]->(tail)
    """

    if 'properties' in triple:
        query += "SET r += $properties"

    session.run(query, {
        'head_id': triple['head'],
        'tail_id': triple['tail'],
        'relation': triple['relation'],
        'properties': triple.get('properties', {})
    })
```

### Step 3：保留原文層（0 天，不用做）

你的原文層已經有了，不用動！

### Step 4：測試雙層查詢（1 天）

```cypher
// 查詢 1：找出闖紅燈的案例，並展示原文
MATCH (c:Case)-[:involves_violation]->(v:Violation {type: '闖紅燈'})
MATCH (c)-[:HAS_LAWYER_INPUT]->(li)
RETURN c.case_id, li.description
LIMIT 10

// 查詢 2：統計骨折的平均賠償
MATCH (c:Case)-[:caused_injury]->(i:Injury {type: '骨折'})
MATCH (c)-[:awards]->(comp {item: '醫療費'})
RETURN avg(comp.amount) as avg_medical_cost

// 查詢 3：Step 5 的相似度計算（用三元組層）
MATCH (c1:Case {case_id: 3062})
MATCH (c2:Case)
WHERE c1 <> c2

// 計算法條 Jaccard
MATCH (c1)-[:cites_law]->(law1)
MATCH (c2)-[:cites_law]->(law2)
WITH c1, c2,
     collect(DISTINCT law1) as laws1,
     collect(DISTINCT law2) as laws2

RETURN c2.case_id,
       size([x IN laws1 WHERE x IN laws2]) * 1.0 /
       size(laws1 + [x IN laws2 WHERE NOT x IN laws1]) as law_jaccard
ORDER BY law_jaccard DESC
LIMIT 5
```

---

## 六、給老師看的說明

### 如果老師問：「你為什麼要重構成三元組？」

**回答**：
「TAARN 論文啟發我，知識圖譜的威力在於『結構化推理』，而不只是『儲存文本』。

**目前的問題**：
- 我的圖譜存了很多原文，雖然方便檢索，但無法做結構化推理
- 例如：想找『闖紅燈導致骨折的案例』，只能用文字搜尋，不能用圖的推理能力

**解決方案**：
- 我借鑒 TAARN 的三元組設計，把關鍵資訊提取成 (實體-關係-實體) 的形式
- 例如：`(案例3062)-[違規行為]->(闖紅燈)-[導致傷害]->(骨折)`
- 這樣就能做路徑推理和統計分析

**創新點**：
- 我用**混合式設計**：原文層（用於展示）+ 三元組層（用於推理）
- 這樣兼顧了實用性和學術性，是對 TAARN 的改進」

### 如果老師問：「這樣工作量會不會太大？」

**回答**：
「不會太大。因為我在 Step 2-4 已經用 LLM 提取了結構化資訊（`extracted_info`），現在只需要把這些資訊轉換成三元組的形式，預計 5-7 天可以完成。

而且，我採用漸進式的方法：
1. 先建立關鍵的三元組（法條、賠償項目、違規行為）
2. 保留原文層，不刪除舊資料（風險低）
3. 未來可以逐步擴充更多三元組

這樣既能展示『酷酷的推理』，又不會影響現有系統的運作。」

---

## 七、最終建議

### 我的建議：先做方案 3，再升級到方案 2

**理由**：
1. **快速驗證**：用 3-4 天完成方案 3，驗證三元組的可行性
2. **給老師展示**：展示基本的三元組推理（足夠酷了）
3. **評估反饋**：看老師的反應，決定是否要升級到方案 2
4. **降低風險**：如果老師不喜歡，只浪費 3-4 天，而不是 7-10 天

### 時間規劃

**Week 1（3-4 天）**：
- 完成方案 3（局部重構）
- 跟老師討論，展示三元組推理

**Week 2（5-7 天）**：
- 如果老師滿意 → 繼續執行 Step 5
- 如果老師想要更多推理能力 → 升級到方案 2

**這樣最穩！**

---

## 八、需要我幫你做什麼？

我可以幫你：
1. ✅ 設計詳細的三元組 Schema（定義所有實體和關係）
2. ✅ 寫 `KG_370_build_triples.py`（從 extracted_info 提取三元組）
3. ✅ 寫測試查詢（驗證三元組推理能力）
4. ✅ 準備給老師看的展示範例

你只需要告訴我：
- 你想用哪個方案？（推薦：先方案 3，再考慮方案 2）
- 你有哪些 `extracted_info` 欄位？（我需要看你的結構化資訊）
- 你希望能做哪些「酷酷的推理」？（例如：找相似案例、統計分析、因果推理）

準備好了就告訴我，我們開始重構！💪
