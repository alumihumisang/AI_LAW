# 混合式架構詳細設計：主幹存原文 + Detail存三元組 + 邊權重

## 一、核心設計理念

### 你的理解（完全正確！）

```
【主幹層】：保留原文，用於展示和檢索
Case → LawyerInput_Accident (description: "被告張三於...")
     → LawyerInput_Injury (description: "原告受有...")

【Detail層】：改成三元組，用於推理和計算
Case → Violation (type: "闖紅燈") [邊權重: severity=0.8]
     → Injury (type: "骨折") [邊權重: severity=0.7]
     → Law (article: "民法184-1") [邊權重: importance=1.0]
     → Compensation (item: "醫療費") [邊權重: amount=50000]
```

**優勢**：
1. ✅ 原文不丟失（主幹層保留）
2. ✅ 可以做結構化推理（Detail層三元組）
3. ✅ **邊權重讓相似度計算更精細**（這是你的亮點！）

---

## 二、完整架構設計

### 層次 1：主幹層（文本存儲）

**節點類型**：
```
Case (case_id, liability_type, party_structure)
  ├─ LawyerInput_Accident (description: 完整原文)
  ├─ LawyerInput_Injury (description: 完整原文)
  └─ LawyerInput_Claim (description: 完整原文)
```

**作用**：
- 提供完整的案例原文給使用者閱讀
- 用於展示和驗證
- 作為 LLM 生成回應的素材

**關係**：
```
(Case)-[:HAS_LAWYER_INPUT]->(LawyerInput_Accident)
(LawyerInput_Accident)-[:導致]->(LawyerInput_Injury)
(LawyerInput_Injury)-[:請求]->(LawyerInput_Claim)
```

---

### 層次 2：Detail層（三元組推理）

**節點類型**（實體）：
```
1. Violation (違規行為)
   - type: "闖紅燈" / "超速" / "酒駕" / "未保持安全距離"
   - severity: 0.0-1.0 (嚴重程度)

2. Injury (傷害類型)
   - type: "骨折" / "腦震盪" / "死亡" / "擦傷"
   - severity: 0.0-1.0 (嚴重程度)

3. Law (法條)
   - article: "民法184-1" / "民法191-2"
   - category: "general_tort" / "guardian_liability"
   - importance: 0.0-1.0 (重要性，核心法條=1.0)

4. CompensationItem (賠償項目)
   - item: "醫療費" / "精神慰撫金" / "工作損失"
   - category: "economic" / "non_economic"

5. Vehicle (車輛類型)
   - type: "機車" / "汽車" / "卡車"

6. Location (地點類型)
   - type: "路口" / "高速公路" / "巷道"
```

**關係類型**（帶權重的邊）：
```
1. (Case)-[:INVOLVES_VIOLATION {weight, severity}]->(Violation)
   - weight: 該違規行為在案例中的重要性 (0.0-1.0)
   - severity: 違規行為的嚴重程度 (0.0-1.0)

2. (Case)-[:CAUSED_INJURY {weight, severity}]->(Injury)
   - weight: 該傷害在案例中的重要性 (0.0-1.0)
   - severity: 傷害的嚴重程度 (0.0-1.0)

3. (Case)-[:CITES_LAW {weight, importance, is_primary}]->(Law)
   - weight: 該法條在案例中的重要性 (0.0-1.0)
   - importance: 法條本身的重要性 (0.0-1.0)
   - is_primary: 是否為主要法條 (boolean)

4. (Case)-[:AWARDS {amount, weight}]->(CompensationItem)
   - amount: 賠償金額 (整數)
   - weight: 該項目在總賠償中的佔比 (0.0-1.0)

5. (Case)-[:INVOLVES_VEHICLE {weight}]->(Vehicle)
   - weight: 該車輛在案例中的重要性 (0.0-1.0)
```

---

## 三、「邊上加權重」的威力

### 為什麼要在邊上加權重？

**情境 1：法條重要性不同**

```
案例 A：
  - CITES_LAW → 民法184-1 [importance=1.0, is_primary=True]  ← 核心法條
  - CITES_LAW → 民法195 [importance=0.3, is_primary=False]   ← 輔助法條

案例 B：
  - CITES_LAW → 民法184-1 [importance=1.0, is_primary=True]
  - CITES_LAW → 民法193 [importance=0.3, is_primary=False]

傳統 Jaccard 相似度（無權重）：
  交集 = {184-1}，聯集 = {184-1, 195, 193}
  Jaccard = 1/3 = 0.33

加權 Jaccard 相似度（有權重）：
  交集權重 = 1.0（都有184-1，且都是核心）
  聯集權重 = 1.0 + 0.3 + 0.3 = 1.6
  Weighted Jaccard = 1.0/1.6 = 0.625

結論：加權後更高！因為兩個案例都以民法184-1為核心，輔助法條不同不重要。
```

**情境 2：賠償金額差異**

```
案例 A：
  - AWARDS → 醫療費 [amount=50000, weight=0.33]
  - AWARDS → 精神慰撫金 [amount=100000, weight=0.67]
  - 總賠償：150,000

案例 B：
  - AWARDS → 醫療費 [amount=500000, weight=0.83]
  - AWARDS → 精神慰撫金 [amount=100000, weight=0.17]
  - 總賠償：600,000

傳統方法：
  都有「醫療費」和「精神慰撫金」→ 完全相同 (Jaccard=1.0)

加權方法：
  案例A：主要是精神慰撫金（67%）
  案例B：主要是醫療費（83%）
  → 雖然項目相同，但結構不同！

加權相似度：
  醫療費差異 = |0.33 - 0.83| = 0.5
  精神慰撫金差異 = |0.67 - 0.17| = 0.5
  平均差異 = 0.5
  相似度 = 1 - 0.5 = 0.5

結論：加權後能反映「賠償結構」的差異！
```

**情境 3：違規行為嚴重程度**

```
案例 A：
  - INVOLVES_VIOLATION → 酒駕 [severity=1.0]
  - INVOLVES_VIOLATION → 超速 [severity=0.6]

案例 B：
  - INVOLVES_VIOLATION → 酒駕 [severity=1.0]
  - INVOLVES_VIOLATION → 未保持安全距離 [severity=0.3]

傳統方法：
  交集 = {酒駕}，聯集 = {酒駕, 超速, 未保持安全距離}
  Jaccard = 1/3 = 0.33

加權方法：
  交集權重 = 1.0（都有酒駕，且都是最嚴重）
  聯集權重 = 1.0 + 0.6 + 0.3 = 1.9
  Weighted Jaccard = 1.0/1.9 = 0.526

結論：因為兩個案例都以「酒駕」為主要違規（最嚴重），次要違規不同不影響太多。
```

---

## 四、Step 5 的相似度計算（帶權重版本）

### 原本的公式（無權重）

```
Total_Similarity = 0.60 × Type_Similarity
                 + 0.20 × Law_Jaccard
                 + 0.15 × Compensation_Jaccard
                 + 0.05 × Keyword_Jaccard
```

### 升級版公式（有權重）

```
Total_Similarity = 0.60 × Type_Similarity（不變）
                 + 0.20 × Weighted_Law_Jaccard  ← 考慮法條重要性
                 + 0.15 × Weighted_Comp_Jaccard ← 考慮賠償金額和佔比
                 + 0.05 × Weighted_Violation_Jaccard ← 考慮違規嚴重程度
```

### 具體計算範例

#### 案例 A 的資料

```
案例類型：
  - liability_type: general_tort_184
  - party_structure: single_vs_single

違規行為（三元組）：
  (Case_A)-[:INVOLVES_VIOLATION {severity: 0.8}]->(闖紅燈)

傷害（三元組）：
  (Case_A)-[:CAUSED_INJURY {severity: 0.7}]->(骨折)

法條（三元組）：
  (Case_A)-[:CITES_LAW {importance: 1.0, is_primary: true}]->(民法184-1)
  (Case_A)-[:CITES_LAW {importance: 0.5, is_primary: false}]->(民法191-2)
  (Case_A)-[:CITES_LAW {importance: 0.3, is_primary: false}]->(民法195)

賠償（三元組）：
  (Case_A)-[:AWARDS {amount: 50000, weight: 0.33}]->(醫療費)
  (Case_A)-[:AWARDS {amount: 100000, weight: 0.67}]->(精神慰撫金)
```

#### 案例 B 的資料

```
案例類型：
  - liability_type: general_tort_184  ← 相同
  - party_structure: single_vs_single ← 相同

違規行為：
  (Case_B)-[:INVOLVES_VIOLATION {severity: 0.8}]->(闖紅燈)  ← 相同

傷害：
  (Case_B)-[:CAUSED_INJURY {severity: 0.6}]->(擦傷)  ← 不同（且較輕）

法條：
  (Case_B)-[:CITES_LAW {importance: 1.0, is_primary: true}]->(民法184-1)  ← 相同
  (Case_B)-[:CITES_LAW {importance: 0.5, is_primary: false}]->(民法191-2) ← 相同
  (Case_B)-[:CITES_LAW {importance: 0.3, is_primary: false}]->(民法193)   ← 不同

賠償：
  (Case_B)-[:AWARDS {amount: 20000, weight: 0.67}]->(醫療費)  ← 金額不同，佔比相反
  (Case_B)-[:AWARDS {amount: 10000, weight: 0.33}]->(精神慰撫金)
```

#### 計算過程

**維度 1：案例類型相似度（0.60權重）**
```
liability_type 相同？ Yes → 1.0
party_structure 相同？ Yes → 1.0

Type_Similarity = 1.0
```

**維度 2：加權法條 Jaccard（0.20權重）**
```
案例A的法條權重：{184-1: 1.0, 191-2: 0.5, 195: 0.3}
案例B的法條權重：{184-1: 1.0, 191-2: 0.5, 193: 0.3}

交集法條的權重和：
  184-1: min(1.0, 1.0) = 1.0
  191-2: min(0.5, 0.5) = 0.5
  總和 = 1.5

聯集法條的權重和：
  184-1: max(1.0, 1.0) = 1.0
  191-2: max(0.5, 0.5) = 0.5
  195: 0.3 (只在A)
  193: 0.3 (只在B)
  總和 = 2.1

Weighted_Law_Jaccard = 1.5 / 2.1 = 0.714
```

**維度 3：加權賠償 Jaccard（0.15權重）**
```
方法1：考慮項目重疊（無視金額）
  交集：{醫療費, 精神慰撫金}
  → 完全重疊 = 1.0

方法2：考慮金額結構（你的創新！）
  案例A：醫療費佔33%，精神慰撫金佔67%
  案例B：醫療費佔67%，精神慰撫金佔33%

  結構差異：
    醫療費：|0.33 - 0.67| = 0.34
    精神慰撫金：|0.67 - 0.33| = 0.34
    平均差異 = 0.34

  結構相似度 = 1 - 0.34 = 0.66

方法3：考慮金額絕對值（更精細！）
  案例A總賠償：150,000
  案例B總賠償：30,000

  比例 = 30,000 / 150,000 = 0.2

  金額相似度 = 0.2（差很多！）

綜合（權重平均）：
  Weighted_Comp_Jaccard = 0.5 × 1.0（項目重疊）
                        + 0.3 × 0.66（結構相似）
                        + 0.2 × 0.2（金額相似）
                        = 0.5 + 0.198 + 0.04
                        = 0.738
```

**維度 4：違規行為 Jaccard（0.05權重）**
```
交集：{闖紅燈}，嚴重程度都是 0.8
Weighted_Violation_Jaccard = 1.0
```

**最終總分**
```
Total_Similarity = 0.60 × 1.0    (案例類型)
                 + 0.20 × 0.714  (法條)
                 + 0.15 × 0.738  (賠償)
                 + 0.05 × 1.0    (違規)
                 = 0.60 + 0.143 + 0.111 + 0.05
                 = 0.904（非常相似！）
```

---

## 五、實作：如何建立這個架構

### Step 1：保留主幹層（已完成！）

你的主幹層已經有了，不用動：
```python
# 已經有的節點
Case
LawyerInput_Accident
LawyerInput_Injury
LawyerInput_Claim
```

### Step 2：建立 Detail層的三元組節點

#### 2.1 建立實體節點

```python
# KG_380_build_entity_nodes.py

def create_entity_nodes(session):
    """
    建立所有實體節點（共享節點，不是每個案例都建一個）
    """

    # 1. 建立違規行為節點（全局共享）
    violations = [
        {"type": "闖紅燈", "severity": 0.8},
        {"type": "超速", "severity": 0.6},
        {"type": "酒駕", "severity": 1.0},
        {"type": "未保持安全距離", "severity": 0.4},
        {"type": "違規變換車道", "severity": 0.5}
    ]

    for v in violations:
        session.run("""
            MERGE (v:Violation {type: $type})
            SET v.severity = $severity
        """, v)

    # 2. 建立傷害類型節點
    injuries = [
        {"type": "死亡", "severity": 1.0},
        {"type": "重傷", "severity": 0.9},
        {"type": "骨折", "severity": 0.7},
        {"type": "腦震盪", "severity": 0.8},
        {"type": "擦傷", "severity": 0.3},
        {"type": "挫傷", "severity": 0.4}
    ]

    for i in injuries:
        session.run("""
            MERGE (i:Injury {type: $type})
            SET i.severity = $severity
        """, i)

    # 3. 建立法條節點
    laws = [
        {"article": "民法184-1", "category": "general_tort", "importance": 1.0},
        {"article": "民法184-2", "category": "general_tort", "importance": 0.9},
        {"article": "民法185", "category": "joint_tort", "importance": 0.8},
        {"article": "民法187", "category": "guardian_liability", "importance": 1.0},
        {"article": "民法188", "category": "employer_liability", "importance": 1.0},
        {"article": "民法191-2", "category": "vehicle_liability", "importance": 0.9},
        {"article": "民法193", "category": "compensation_medical", "importance": 0.5},
        {"article": "民法195", "category": "compensation_emotional", "importance": 0.5}
    ]

    for law in laws:
        session.run("""
            MERGE (l:Law {article: $article})
            SET l.category = $category,
                l.importance = $importance
        """, law)

    # 4. 建立賠償項目節點
    compensation_items = [
        {"item": "醫療費", "category": "economic"},
        {"item": "精神慰撫金", "category": "non_economic"},
        {"item": "工作損失", "category": "economic"},
        {"item": "看護費", "category": "economic"},
        {"item": "交通費", "category": "economic"},
        {"item": "喪葬費", "category": "economic"}
    ]

    for comp in compensation_items:
        session.run("""
            MERGE (c:CompensationItem {item: $item})
            SET c.category = $category
        """, comp)

    logger.info("✅ 所有實體節點建立完成！")
```

#### 2.2 從現有資料提取並建立關係

```python
# KG_390_build_weighted_relations.py

def extract_and_link_case(case_id):
    """
    為每個案例建立帶權重的關係
    """
    with driver.session() as session:
        # 1. 提取法條（從 LawDetail）
        laws_result = session.run("""
            MATCH (ld:LawDetail {case_id: $case_id})
            RETURN DISTINCT ld.article as article
        """, case_id=case_id)

        laws = [record['article'] for record in laws_result if record['article']]

        # 判斷主要法條（假設第一條是主要的）
        for i, law_article in enumerate(laws):
            is_primary = (i == 0)

            # 建立關係
            session.run("""
                MATCH (c:Case {case_id: $case_id})
                MATCH (l:Law {article: $law_article})
                MERGE (c)-[r:CITES_LAW]->(l)
                SET r.is_primary = $is_primary,
                    r.importance = l.importance,
                    r.weight = CASE WHEN $is_primary THEN 1.0 ELSE 0.5 END
            """, case_id=case_id, law_article=law_article, is_primary=is_primary)

        # 2. 提取賠償項目（從 CompensationDetail 的 extracted_info）
        comp_result = session.run("""
            MATCH (cd:CompensationDetail {case_id: $case_id})
            RETURN cd.extracted_info as info
        """, case_id=case_id)

        compensations = []
        total_amount = 0

        for record in comp_result:
            if record['info']:
                info = json.loads(record['info'])
                if 'item' in info and 'amount' in info:
                    compensations.append({
                        'item': info['item'],
                        'amount': info['amount']
                    })
                    total_amount += info['amount']

        # 計算權重（佔比）
        for comp in compensations:
            weight = comp['amount'] / total_amount if total_amount > 0 else 0

            session.run("""
                MATCH (c:Case {case_id: $case_id})
                MATCH (ci:CompensationItem {item: $item})
                MERGE (c)-[r:AWARDS]->(ci)
                SET r.amount = $amount,
                    r.weight = $weight
            """, case_id=case_id, item=comp['item'], amount=comp['amount'], weight=weight)

        # 3. 提取違規行為（從 FactDetail 的 chunk_text，用規則或 LLM）
        # 這部分需要你的 extracted_info 裡有違規行為資訊
        # 或者用簡單的規則匹配

        fact_result = session.run("""
            MATCH (fd:FactDetail {case_id: $case_id})
            RETURN fd.chunk_text as text
            LIMIT 5
        """, case_id=case_id)

        fact_texts = [record['text'] for record in fact_result if record['text']]
        combined_text = ' '.join(fact_texts)

        # 簡單規則匹配（你可以用更複雜的方法）
        violations_detected = []
        if "闖紅燈" in combined_text or "紅燈" in combined_text:
            violations_detected.append("闖紅燈")
        if "超速" in combined_text:
            violations_detected.append("超速")
        if "酒駕" in combined_text or "飲酒" in combined_text:
            violations_detected.append("酒駕")

        for violation in violations_detected:
            session.run("""
                MATCH (c:Case {case_id: $case_id})
                MATCH (v:Violation {type: $violation})
                MERGE (c)-[r:INVOLVES_VIOLATION]->(v)
                SET r.severity = v.severity,
                    r.weight = 1.0
            """, case_id=case_id, violation=violation)

        # 4. 提取傷害類型（同樣從 FactDetail）
        injuries_detected = []
        if "骨折" in combined_text:
            injuries_detected.append("骨折")
        if "死亡" in combined_text or "身亡" in combined_text:
            injuries_detected.append("死亡")
        if "腦震盪" in combined_text:
            injuries_detected.append("腦震盪")
        if "擦傷" in combined_text:
            injuries_detected.append("擦傷")

        for injury in injuries_detected:
            session.run("""
                MATCH (c:Case {case_id: $case_id})
                MATCH (i:Injury {type: $injury})
                MERGE (c)-[r:CAUSED_INJURY]->(i)
                SET r.severity = i.severity,
                    r.weight = 1.0
            """, case_id=case_id, injury=injury)

def process_all_cases():
    """
    處理所有 6057 個案例
    """
    with driver.session() as session:
        result = session.run("""
            MATCH (c:Case)
            RETURN c.case_id as case_id
            ORDER BY c.case_id
        """)

        case_ids = [record['case_id'] for record in result]

    logger.info(f"📊 開始處理 {len(case_ids)} 個案例...")

    for i, case_id in enumerate(case_ids, 1):
        extract_and_link_case(case_id)

        if i % 500 == 0:
            logger.info(f"✅ 已處理 {i}/{len(case_ids)} 個案例")

    logger.info("🎉 所有案例處理完成！")
```

### Step 3：計算加權相似度

```python
# KG_500_build_weighted_similarity.py

def calculate_weighted_law_jaccard(case1_id, case2_id):
    """
    計算加權法條 Jaccard 相似度
    """
    with driver.session() as session:
        result = session.run("""
            // 取得案例1的法條權重
            MATCH (c1:Case {case_id: $case1_id})-[r1:CITES_LAW]->(l1:Law)
            WITH collect({law: l1.article, weight: r1.importance}) as laws1

            // 取得案例2的法條權重
            MATCH (c2:Case {case_id: $case2_id})-[r2:CITES_LAW]->(l2:Law)
            WITH laws1, collect({law: l2.article, weight: r2.importance}) as laws2

            // 計算交集權重
            WITH laws1, laws2,
                 [l IN laws1 WHERE l.law IN [x.law FOR x IN laws2] |
                  {law: l.law, weight: apoc.coll.min([l.weight,
                    [x IN laws2 WHERE x.law = l.law | x.weight][0]
                  ])}
                 ] as intersection

            // 計算聯集權重
            WITH laws1, laws2, intersection,
                 laws1 + [l IN laws2 WHERE NOT l.law IN [x.law FOR x IN laws1]] as union

            // 計算加權 Jaccard
            RETURN
                reduce(s = 0.0, i IN intersection | s + i.weight) as intersection_weight,
                reduce(s = 0.0, u IN union | s + u.weight) as union_weight
        """, case1_id=case1_id, case2_id=case2_id)

        record = result.single()

        if record['union_weight'] == 0:
            return 0.0

        return record['intersection_weight'] / record['union_weight']

def calculate_total_weighted_similarity(case1_id, case2_id):
    """
    計算總相似度（帶權重版本）
    """
    # 1. 案例類型相似度（0.60）
    type_sim = calculate_type_similarity(case1_id, case2_id)

    # 2. 加權法條 Jaccard（0.20）
    law_sim = calculate_weighted_law_jaccard(case1_id, case2_id)

    # 3. 加權賠償 Jaccard（0.15）
    comp_sim = calculate_weighted_compensation_jaccard(case1_id, case2_id)

    # 4. 加權違規 Jaccard（0.05）
    viol_sim = calculate_weighted_violation_jaccard(case1_id, case2_id)

    # 總分
    total_sim = (0.60 * type_sim +
                 0.20 * law_sim +
                 0.15 * comp_sim +
                 0.05 * viol_sim)

    return {
        'total': total_sim,
        'type': type_sim,
        'law': law_sim,
        'compensation': comp_sim,
        'violation': viol_sim
    }
```

---

## 六、這個架構的優勢總結

### 對比傳統方法

| 維度 | 傳統方法（無權重） | 混合式架構（有權重） |
|------|-------------------|---------------------|
| **法條相似度** | 只看有沒有重疊 | 考慮法條重要性 |
| **賠償相似度** | 只看項目相同 | 考慮金額和佔比 |
| **違規相似度** | 只看類型相同 | 考慮嚴重程度 |
| **推理能力** | 無法推理 | 可以做路徑推理 |
| **展示能力** | 只有原文 | 原文+結構化資訊 |
| **可解釋性** | 中等 | 高（可以說明每個權重） |

### 給老師展示的亮點

1. **多層設計**：主幹層保留原文 + Detail層結構化推理
2. **邊權重創新**：不只是有/無的二元關係，而是考慮重要性程度
3. **精細計算**：加權 Jaccard 比傳統 Jaccard 更準確
4. **可解釋性**：可以說明「為什麼這兩個案例相似度是 0.9」
   - 案例類型相同（60%）✅
   - 核心法條相同（20%）✅
   - 賠償結構相似（15%）✅
   - 違規行為相同（5%）✅

---

## 七、時間規劃

| 階段 | 任務 | 時間 | 產出 |
|------|------|------|------|
| **Day 1** | 設計 Schema 和建立實體節點 | 1 天 | Violation, Injury, Law, CompensationItem 節點 |
| **Day 2-3** | 提取並建立關係（6057 案例） | 2 天 | 所有 CITES_LAW, AWARDS 等關係 |
| **Day 4** | 實作加權相似度計算 | 1 天 | 加權 Jaccard 函數 |
| **Day 5** | 測試和驗證 | 1 天 | 驗證相似度計算正確性 |
| **Day 6-7** | 建立 SIMILAR_CASE 邊（Step 5） | 2 天 | ~15,000 條相似度邊 |

**總計：6-7 天**

---

## 八、需要我幫你做什麼？

我可以立刻幫你：

1. ✅ 寫完整的 `KG_380_build_entity_nodes.py`（建立所有實體節點）
2. ✅ 寫完整的 `KG_390_build_weighted_relations.py`（提取並建立關係）
3. ✅ 寫完整的 `KG_500_build_weighted_similarity.py`（計算加權相似度）
4. ✅ 準備給老師看的展示範例（酷酷的查詢）

你準備好開始了嗎？還是想再討論一下細節？🚀
