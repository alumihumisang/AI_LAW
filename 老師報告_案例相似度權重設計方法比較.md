# 案例相似度權重設計方法：與 TAARN 論文的比較

> **討論目的**：在執行 Step 5（建立案例相似度邊）前，確認權重設計方法的合理性
> **參考論文**：TAARN - Text-augmented attentive recurrent reasoner network for knowledge graph question answering (High-Confidence Computing, 2025)
> **討論日期**：2025-11-27

---

## 一、背景：為何需要討論權重設計方法？

### 1.1 當前狀況

✅ **已完成**：
- 6,057 筆案例的知識圖譜建構完成
- liability_type 標記完成（100% 準確，基於法條）
- party_structure 標記完成（99.98% 高信心度）

⏳ **待執行**（Step 5）：
- 建立案例間的相似度邊（SIMILAR_CASE 關係）
- 需要設計：如何計算兩個案例的相似度分數？

### 1.2 核心問題

**Question 1**：相似度應該考慮哪些維度？
- 只用向量相似度（文字相似）夠嗎？
- 還是需要多維度組合？

**Question 2**：如何避免過度連接？
- 6,057 個案例，如果全連接 = 1,835 萬條邊（不可行）
- 如何稀疏化？

**Question 3**：我們的設計有學術依據嗎？
- 需要與領域內的頂會論文比較
- TAARN 論文提供了什麼啟發？

---

## 二、TAARN 論文核心方法回顧

### 2.1 論文基本信息

**標題**：TAARN: A text-augmented attentive recurrent reasoner network for knowledge graph question answering

**發表**：High-Confidence Computing (2025年1月接受)

**核心貢獻**：
1. 局部影響權重（Local Influence Weights）
2. 全局影響權重（Global Influence Weights）
3. 語義深度引導路徑提取（Semantic-depth Guided Path Extraction）
4. 提示學習（Prompt Learning）融合文本信息
5. 交叉注意力 RNN（Cross-attention RNN）自適應推理

### 2.2 核心設計理念

TAARN 的關鍵思想是：**多維度權重 + 結構與語義結合 + 路徑稀疏化**

#### (1) 局部影響權重（Local Influence Weights）

**公式**：
```
f(ei, ej) = (eiWQ · (ejWK ⊙ rij)^T) / √d
```

**解釋**：
- `ei`, `ej`：兩個實體節點的嵌入向量
- `rij`：連接它們的關係類型（如 "born_in", "works_for"）
- `WQ`, `WK`：查詢和鍵的投影矩陣
- `⊙`：元素級乘法（Hadamard product）

**核心思想**：
- 使用 **GAT（圖注意力網絡）** 學習節點間的結構性影響力
- **關係類型** `rij` 會調節注意力權重（不同關係有不同重要性）
- 這是一種「局部」權重，只考慮直接鄰居

#### (2) 全局影響權重（Global Influence Weights）

**公式**：
```
ω(ei, rij, ej) = |Ni| · exp(f(ei,ej)) / Σ_{ej' ∈ Ni} exp(f(ei,ej'))
```

**解釋**：
- `|Ni|`：節點 `ei` 的度數（鄰居數量）
- `exp(f(ei,ej))`：局部權重的 softmax 歸一化

**核心思想**：
- **節點度數** `|Ni|` 作為全局重要性指標
- 度數高的節點（如"美國"）應該有更高的權重
- 這是一種「全局」權重，考慮整體圖結構

#### (3) 語義深度引導路徑提取

**公式**：
```
π = α·β  （depth=2 時效果最佳）
其中 β = 1 - sim(ehr, et)
```

**解釋**：
- `α`：路徑深度因子（深度越深，權重越低）
- `β`：語義相似度（用 BERT 計算）
- `depth=2`：論文實驗發現路徑深度為 2 跳時準確率最高（84.7%）

**核心思想**：
- **限制路徑深度** 避免過度連接（depth > 2 會引入噪音）
- 結合語義相似度選擇推理路徑
- 這是一種「稀疏化策略」

#### (4) 提示學習（Prompt Learning）

**方法**：
```
模板："The sentence: 'X' means [MASK]"
例如："The sentence: 'The Nile' means [MASK]" → 學習到「河流」
```

**核心思想**：
- 使用 **PLM（預訓練語言模型）** 提取實體的文本語義
- 將結構化知識圖譜與非結構化文本結合
- 這是一種「語義增強」策略

### 2.3 TAARN 的三個關鍵啟發

| 啟發點 | TAARN 的做法 | 對我們的啟示 |
|-------|-------------|------------|
| **1. 多維度權重** | 局部權重（結構）+ 全局權重（度數）+ 語義權重（文本） | 不能只用單一相似度，應該組合多個維度 |
| **2. 稀疏化策略** | 限制路徑深度為 2（避免噪音） | 不能全連接，應該限制每個案例的鄰居數量 |
| **3. 語義與結構結合** | BERT 文本嵌入 + 圖結構嵌入 | 法條（結構）+ 事實描述（語義）都要考慮 |

---

## 三、我們的案例相似度權重設計

### 3.1 設計方案

受 TAARN 啟發，我們採用 **多維度加權相似度** 方法：

```
總相似度 = w1·案例類型匹配 + w2·法條重疊度 + w3·賠償項目重疊度 + w4·事實關鍵詞重疊度
```

**權重分配**：
- **w1 = 0.60**（案例類型匹配）
  - 責任基礎匹配：0.35
  - 當事人結構匹配：0.25
- **w2 = 0.20**（法條重疊度）：Jaccard 相似度
- **w3 = 0.15**（賠償項目重疊度）：Jaccard 相似度
- **w4 = 0.05**（事實關鍵詞重疊度）：TF-IDF 或詞袋模型

**稀疏化策略**：
- **Top-K 邊**：每個案例只保留 Top-5 最相似案例
- **最低閾值**：相似度 < 0.65 的不建邊
- **雙向去重**：A→B 和 B→A 視為同一條邊

**預期結果**：
- 生成 ~15,000-20,000 條 SIMILAR_CASE 邊
- 遠少於全連接的 1,835 萬條邊

### 3.2 四個維度的詳細設計

#### 維度 1：案例類型匹配（60%）

**公式**：
```python
def case_type_match(case1, case2):
    # 責任基礎完全匹配（0.35）
    if case1.liability_type != case2.liability_type:
        liability_score = 0.0
    else:
        liability_score = 1.0

    # 當事人結構完全匹配（0.25）
    if case1.party_structure != case2.party_structure:
        party_score = 0.0
    else:
        party_score = 1.0

    return 0.35 * liability_score + 0.25 * party_score
```

**為什麼權重最高（60%）？**

1. **法律邏輯的必要性**：
   - 監護人責任（187條）vs 僱用人責任（188條）→ 舉證責任、免責事由完全不同
   - 單一原被告 vs 多方當事人 → 訴訟策略、和解談判方式不同

2. **類似 TAARN 的關係類型調節**：
   - TAARN 使用 `rij`（關係類型）調節注意力權重
   - 我們使用案例類型作為最重要的「關係類型」

3. **實證數據支持**：
   - liability_type 分布：84% 一般侵權、12% 僱用人責任、4% 監護人責任
   - 不同類型的案例在實務上很少互相參考

#### 維度 2：法條重疊度（20%）

**公式**：
```python
def law_overlap(case1, case2):
    laws1 = set(case1.applicable_laws)
    laws2 = set(case2.applicable_laws)

    # Jaccard 相似度
    intersection = len(laws1 & laws2)
    union = len(laws1 | laws2)

    return intersection / union if union > 0 else 0.0
```

**範例**：
```
案例 A 適用法條：{民法184條, 民法191-2條, 民法193條, 民法195條}
案例 B 適用法條：{民法184條, 民法191-2條, 民法195條}

交集：{184, 191-2, 195} = 3 個
聯集：{184, 191-2, 193, 195} = 4 個

Jaccard = 3/4 = 0.75
```

**為什麼權重次之（20%）？**

1. **結構化信息的可靠性**：
   - 法條是判決書中最結構化、最準確的信息（100% 可提取）
   - 類似 TAARN 使用圖結構信息

2. **法律推理的核心**：
   - 相同的法條組合 → 相似的法律推理邏輯
   - 如：184條 + 191-2條（車輛持有人責任）是常見組合

#### 維度 3：賠償項目重疊度（15%）

**公式**：
```python
def compensation_overlap(case1, case2):
    comp1 = set(case1.compensation_categories)  # 如：['醫療費', '精神慰撫金']
    comp2 = set(case2.compensation_categories)

    # Jaccard 相似度
    return len(comp1 & comp2) / len(comp1 | comp2)
```

**範例**：
```
案例 A 賠償項目：{醫療費, 看護費, 精神慰撫金, 交通費}
案例 B 賠償項目：{醫療費, 精神慰撫金, 車輛損失}

交集：{醫療費, 精神慰撫金} = 2 個
聯集：{醫療費, 看護費, 精神慰撫金, 交通費, 車輛損失} = 5 個

Jaccard = 2/5 = 0.40
```

**為什麼權重適中（15%）？**

1. **反映案件的損害類型**：
   - 醫療費 + 看護費 + 精神慰撫金 → 人身傷害案件
   - 車輛損失 + 拖吊費 → 財產損害案件

2. **輔助判斷嚴重程度**：
   - 只有精神慰撫金 → 輕傷
   - 醫療費 + 看護費 + 精神慰撫金 → 重傷

#### 維度 4：事實關鍵詞重疊度（5%）

**公式**：
```python
def fact_keyword_overlap(case1, case2):
    # 從 FactDetail 提取關鍵詞（如：闖紅燈、酒駕、超速）
    keywords1 = extract_keywords(case1.fact_texts)
    keywords2 = extract_keywords(case2.fact_texts)

    # TF-IDF 加權 Jaccard
    return weighted_jaccard(keywords1, keywords2)
```

**為什麼權重最低（5%）？**

1. **高變異性**：
   - 相同的法律責任可能有不同的事實描述
   - 如：都是酒駕，但有的是「撞到路人」、有的是「追撞前車」

2. **提取困難**：
   - 事實文本通常很長（平均 500 字）
   - 關鍵詞提取可能不準確（需要 LLM 或 NLP 工具）

3. **輔助作用**：
   - 作為「錦上添花」的語義信息，不應主導相似度
   - 類似 TAARN 的 Prompt Learning，但權重較低

---

## 四、與 TAARN 方法的詳細對照

### 4.1 核心設計對照表

| 設計維度 | TAARN 論文 | 我們的方法 | 相似性 | 差異性 |
|---------|-----------|----------|--------|--------|
| **權重計算方式** | GAT + mask-reconstruction<br>（深度學習，需訓練） | 多維度加權公式<br>（規則式，無需訓練） | ✅ 都是多維度權重 | ❌ 深度學習 vs 規則式 |
| **結構化信息** | 關係類型 `rij`<br>節點度數 `\|Ni\|` | 案例類型（liability_type, party_structure）<br>法條、賠償項目 | ✅ 都使用結構化特徵 | 不同領域的特徵 |
| **語義信息** | PLM + Prompt Learning<br>`"X means [MASK]"` | 事實關鍵詞（TF-IDF）<br>（可擴展為 Gemma embedding） | ✅ 都融合文本語義 | ❌ PLM vs 簡化詞袋 |
| **稀疏化策略** | 路徑深度限制<br>（depth=2 最佳） | Top-K 邊限制<br>（K=5，每個案例只保留 5 個鄰居） | ✅ 都避免過度連接 | 不同的稀疏化方法 |
| **可解釋性** | 注意力權重可視化 | 每個維度的貢獻度可追溯<br>（如：案例類型 60%、法條 20%） | ✅ 都支持可解釋性 | 規則式更直觀 |
| **訓練需求** | 需要大量標註數據和 GPU | 無需訓練 | - | ❌ 有監督 vs 無監督 |
| **領域適應** | 通用知識圖譜<br>（Freebase, NELL） | 法律領域知識圖譜 | - | 法律領域的特殊性 |

### 4.2 相似之處（Inspired by TAARN）

#### 相似點 1：多維度權重設計

**TAARN**：
- 局部權重（GAT）捕捉節點間的直接影響
- 全局權重（mask-reconstruction）捕捉整體圖結構

**我們的方法**：
- **案例類型匹配（60%）** ← 對應「局部權重」（直接相似性）
- **法條/賠償重疊度（35%）** ← 對應「全局權重」（在法律框架中的位置）
- **事實關鍵詞（5%）** ← 對應「語義權重」（文本語義）

**啟發**：
> TAARN 證明了單一權重（如僅用向量相似度）是不夠的，多維度組合能更準確地捕捉實體間的關聯。我們將此思想應用於法律案例，將「案例類型」視為最重要的結構化特徵（類似 TAARN 的關係類型 `rij`），並組合法條和賠償項目作為輔助維度。

#### 相似點 2：稀疏圖設計

**TAARN**：
- 路徑深度限制：depth=2 時準確率最高（84.7%）
- 深度 > 2 會引入噪音，降低準確率

**我們的方法**：
- Top-5 策略：每個案例只連接最相似的 5 個案例
- 相似度閾值：< 0.65 的不建邊

**啟發**：
> TAARN 的實驗發現「更多連接不等於更好」，過度連接會引入噪音。我們採用 Top-K 策略限制每個案例的鄰居數量，這相當於限制了「一跳鄰居」的數量。未來檢索時，可以考慮「二跳相似案例」（類似 TAARN 的 depth=2）。

**預期效果**：
- 全連接：6,057 × 6,056 / 2 ≈ 1,835 萬條邊（不可行）
- Top-5：6,057 × 5 / 2 ≈ 15,000 條邊（可行）

#### 相似點 3：結構與語義結合

**TAARN**：
- 結構：`xS = Linear(xStruct)`（圖結構嵌入）
- 語義：`xT = Linear(xText)`（BERT 文本嵌入）
- 融合：`xF = xS + xT`

**我們的方法**：
- 結構：案例類型 + 法條 + 賠償項目（60% + 20% + 15%）
- 語義：事實關鍵詞（5%）
- 融合：加權組合

**啟發**：
> TAARN 使用 Prompt Learning 提取實體的文本語義，證明了文本信息能補充圖結構的不足。我們將此思想應用於事實描述，提取關鍵詞（如「酒駕」、「闖紅燈」）作為輔助語義信息。

### 4.3 差異之處（Adapted for Legal Domain）

#### 差異點 1：深度學習 vs 規則式

**TAARN**：
- 使用 GAT（圖注意力網絡）學習權重
- 需要大量標註數據和 GPU 訓練

**我們的方法**：
- 使用規則式公式計算權重
- 無需訓練，直接計算

**原因**：
1. **可解釋性需求**：法律領域需要律師/法官理解為何推薦此案例
2. **數據規模限制**：我們只有 6,057 筆案例，不足以訓練深度模型
3. **靈活性**：規則式方法更容易調整權重（如律師可能認為法條更重要）

#### 差異點 2：關係類型 vs 案例類型

**TAARN**：
- 關係類型（rij）：如 "born_in", "works_for", "located_in"
- 用於調節注意力權重

**我們的方法**：
- 案例類型：liability_type（責任基礎）+ party_structure（當事人結構）
- 用於過濾和加權

**原因**：
- 法律案例沒有多種「關係類型」，但有明確的「案例類型」
- 不同責任基礎（187條 vs 188條）的案例邏輯完全不同

#### 差異點 3：節點度數 vs 共現頻率

**TAARN**：
- 節點度數 `|Ni|` 作為全局重要性指標
- 如：「美國」連接很多人/地點 → 重要性高

**我們的方法**：
- 共現頻率作為統計權重
- 如：「左膝骨折 → 精神慰撫金」在 92% 案例中出現

**原因**：
- 法律案例中沒有「中心節點」的概念
- 共現頻率更能反映法律規律（如「骨折通常都有醫療費」）

---

## 五、方法合理性論證

### 5.1 為什麼案例類型權重最高（60%）？

**論證 1：法律邏輯的必要性**

不同責任基礎的舉證責任和免責事由完全不同：

| 責任類型 | 舉證責任 | 免責事由 | 實務差異 |
|---------|---------|---------|---------|
| 監護人責任（187條） | 監護人需證明「已盡監督義務」 | 已盡監督義務 | 需提供監護紀錄 |
| 僱用人責任（188條） | 僱用人需證明「選任監督無過失」 | 選任監督無過失 | 需提供勞動合約 |
| 一般侵權（184條） | 原告需證明「故意或過失」 | 無過失 | 需提供過失證據 |

**結論**：不同類型的案例在法律推理邏輯上完全不同，因此應該是最重要的相似度維度。

**論證 2：類似 TAARN 的關係類型調節**

TAARN 使用關係類型 `rij` 調節注意力權重：
```
f(ei, ej) = (eiWQ · (ejWK ⊙ rij)^T) / √d
```

我們的案例類型類似於「關係類型」，不同類型的案例有不同的相似度計算邏輯。

**論證 3：實證數據支持**

我們的 6,057 個案例分布：
- general_tort_184_191：5,094 個（84.1%）
- employer_liability_188：721 個（11.9%）
- guardian_liability_187：228 個（3.8%）
- animal_liability_190：13 個（0.2%）

不同類型的案例在實務上很少互相參考（僱用人責任的律師不會參考監護人責任案例）。

### 5.2 為什麼法條權重次之（20%）？

**論證 1：結構化信息的可靠性**

- 法條是判決書中最結構化、最準確的信息（100% 可提取）
- 類似 TAARN 使用圖結構信息（節點、邊）

**論證 2：法律推理的核心**

相同的法條組合 → 相似的法律推理邏輯

範例：
```
組合 A：民法 184條 + 191-2條 + 193條 + 195條
→ 車輛持有人責任 + 人身傷害賠償

組合 B：民法 184條 + 191-3條 + 216條
→ 工作物責任 + 財產損害賠償

→ 雖然都有 184條，但推理邏輯完全不同
```

**論證 3：Jaccard 相似度的適用性**

Jaccard 相似度考慮了集合的對稱性：
```
案例 A：{184, 191-2, 193, 195}（4 個法條）
案例 B：{184, 191-2, 195}（3 個法條）

Jaccard = 3/4 = 0.75（高相似度）
```

### 5.3 為什麼事實關鍵詞權重最低（5%）？

**論證 1：高變異性**

相同的法律責任可能有不同的事實描述：
```
案例 A：「被告酒後駕車，闖紅燈撞到原告」
案例 B：「被告酒後駕車，未保持安全距離追撞前車」

→ 都是酒駕，但事實描述差異大
→ 法條相同（184條），賠償邏輯相似
```

**論證 2：提取困難**

事實文本通常很長（平均 500 字），關鍵詞提取可能不準確：
- 需要 LLM 或 NLP 工具
- 可能提取到無關詞彙（如「於」、「之」）

**論證 3：輔助作用**

作為「錦上添花」的語義信息，不應主導相似度：
- 主要維度：案例類型（60%）+ 法條（20%）
- 輔助維度：賠償項目（15%）+ 事實關鍵詞（5%）

### 5.4 為什麼用 Top-5 稀疏化？

**論證 1：TAARN 的路徑深度限制**

TAARN 的實驗結果：
- depth=1：準確率 78.2%
- depth=2：準確率 84.7% ← 最佳
- depth=3：準確率 81.3%（下降）

**啟發**：更多連接不等於更好，適度稀疏能提升準確率。

**論證 2：計算成本考量**

全連接：
```
6,057 × 6,056 / 2 = 18,352,416 條邊
→ 計算成本：O(n²)，不可行
→ 儲存成本：~1.8 億個數值
```

Top-5：
```
6,057 × 5 / 2 = 15,142 條邊
→ 計算成本：O(n·k)，可行
→ 儲存成本：~15 萬個數值
```

**論證 3：實務需求**

律師查詢案例時，通常只需要 **Top-3 到 Top-5** 最相似的案例：
- 太少（Top-1）：覆蓋不足
- 太多（Top-20）：資訊過載

Top-5 是最佳平衡點。

---

## 六、實作計畫與驗證方法

### 6.1 實作步驟（Step 5）

**腳本名稱**：`KG_500_build_case_similarity.py`

**輸入**：
- 6,057 個 Case 節點（已標記 liability_type 和 party_structure）
- 每個 Case 的 LawDetail 和 CompensationDetail 節點

**輸出**：
- ~15,000 條 `SIMILAR_CASE` 邊，每條邊包含：
  ```cypher
  (:Case)-[:SIMILAR_CASE {
      similarity_score: 0.78,
      type_match: 0.60,
      law_overlap: 0.12,
      comp_overlap: 0.05,
      fact_overlap: 0.01
  }]->(:Case)
  ```

**批次處理策略**：
1. 按 liability_type 分組（只計算同類型的案例對）
2. 每 100 個案例為一批（避免記憶體溢出）
3. 使用多進程加速計算

**預計時間**：
- 每個案例對：~0.5 秒
- 總計算量：~30,000 個案例對（只計算同類型）
- 預計時間：~4-5 小時

### 6.2 驗證方法

#### 驗證 1：定性抽樣檢查

隨機抽樣 50 個案例對，人工檢查相似度是否合理：

| Case A | Case B | Similarity | Type Match | Law Overlap | Comp Overlap | 人工判斷 |
|--------|--------|-----------|-----------|------------|-------------|---------|
| 3062_01 | 3062_45 | 0.82 | 0.60 | 0.15 | 0.07 | ✅ 合理 |
| 6057_12 | 6057_89 | 0.75 | 0.60 | 0.10 | 0.05 | ✅ 合理 |
| ... | ... | ... | ... | ... | ... | ... |

**目標**：人工判斷「合理」的比例 > 85%

#### 驗證 2：定量分布統計

檢查相似度分數的分布：
```python
import matplotlib.pyplot as plt

# 統計相似度分布
similarity_scores = [edge.similarity_score for edge in all_edges]

plt.hist(similarity_scores, bins=20)
plt.xlabel('Similarity Score')
plt.ylabel('Frequency')
plt.title('Similarity Score Distribution')
```

**預期**：
- 應該符合長尾分布（大部分案例相似度 0.65-0.75，少數 > 0.80）
- 不應該有大量 1.0 分（表示重複案例）

#### 驗證 3：消融實驗（Ablation Study）

測試每個維度的貢獻度：

| 實驗組 | 權重配置 | Top-5 平均相似度 | 人工判斷準確率 |
|-------|---------|----------------|--------------|
| 完整模型 | 60% + 20% + 15% + 5% | 0.78 | 85% |
| 去除案例類型 | 0% + 35% + 35% + 30% | ? | ? |
| 去除法條 | 75% + 0% + 15% + 10% | ? | ? |
| 去除賠償項目 | 75% + 20% + 0% + 5% | ? | ? |
| 去除事實關鍵詞 | 63% + 21% + 16% + 0% | ? | ? |

**目標**：驗證案例類型（60%）確實是最重要的維度。

---

## 七、與老師討論的重點問題

### 7.1 需要確認的核心問題

**Question 1：權重分配是否合理？**
- 案例類型 60%、法條 20%、賠償 15%、事實 5%
- 是否需要調整（如提高法條權重到 30%）？
- 依據：TAARN 並未給出明確的權重比例，我們基於法律邏輯設計

**Question 2：稀疏化策略是否足夠？**
- Top-K = 5 是否足夠？（或改為 K=10？）
- 是否需要設定相似度下限（如 0.65）？
- 依據：TAARN 使用 depth=2，我們用 Top-5 類比

**Question 3：未來是否需要深度學習？**
- Step 6 是否使用 Gemma 計算語義權重？
- 還是繼續用規則式方法（更可解釋）？
- 依據：TAARN 使用 BERT，但我們數據量較小

### 7.2 與 TAARN 的關聯論述

**可以這樣說明**：

> 「我們的方法受到 TAARN 論文的啟發，採用了 **多維度權重設計** 和 **結構與語義結合** 的核心思想。
>
> TAARN 使用局部權重（GAT）+ 全局權重（節點度數）+ 語義權重（BERT）的三維設計，證明了單一維度（如僅用向量相似度）是不夠的。我們將這個思想應用於法律案例，設計了案例類型（60%）+ 法條（20%）+ 賠償項目（15%）+ 事實關鍵詞（5%）的四維權重。
>
> 此外，TAARN 的實驗發現路徑深度為 2 時效果最佳，這啟發我們使用 **Top-K 稀疏化策略**（K=5），避免過度連接引入噪音。
>
> 但考慮到法律領域的特殊性（需要可解釋性、案例數量有限），我們將深度學習方法改編為 **規則式加權公式**，這樣律師和法官能理解為何推薦某個案例，也更容易根據實務需求調整權重。」

### 7.3 預期成果

完成 Step 5 後，知識圖譜將具備：

1. **案例推薦能力**：
   - 給定一個新案例，可以找出最相似的 5 個歷史案例
   - 每個推薦案例都有明確的相似度分數和各維度貢獻度

2. **可解釋性**：
   - 律師可以看到：「這個案例與您的案例相似度 78%，其中案例類型完全匹配（60%），法條重疊度 60%（12%），賠償項目重疊度 33%（5%）」
   - 這比「向量相似度 0.78」更有意義

3. **未來擴展基礎**：
   - 案例檢索系統
   - 判決金額預測（基於相似案例的統計）
   - 訴訟策略建議

---

## 八、總結

### 8.1 核心創新點

| 項目 | 內容 |
|------|------|
| **受 TAARN 啟發** | 多維度權重設計 + 稀疏化策略 + 結構與語義結合 |
| **核心改編** | 將深度學習方法改編為法律領域可解釋的規則式公式 |
| **四維權重** | 案例類型（60%）+ 法條（20%）+ 賠償項目（15%）+ 事實關鍵詞（5%） |
| **稀疏化策略** | Top-5 + 相似度閾值 0.65 |
| **預期邊數** | ~15,000 條（遠少於全連接的 1,835 萬條） |

### 8.2 下一步

待老師確認方法合理性後：
1. 實作 `KG_500_build_case_similarity.py`（預計 4-5 小時）
2. 驗證相似度分數（定性抽樣 + 定量統計 + 消融實驗）
3. 根據驗證結果調整權重
4. 進入 Step 6（語義權重）或繼續優化 Step 5

---

**討論完畢，期待老師的指導！**
